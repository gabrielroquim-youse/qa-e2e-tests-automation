/* eslint-disable playwright/expect-expect, playwright/no-skipped-test */
/**
 * Arquivo de seed para geração rápida de testes via Playwright codegen.
 *
 * Use este arquivo como ponto de partida ao gravar interações com o navegador:
 *   npx playwright codegen <url>
 * Cole o código gerado aqui, ajuste e mova para o spec definitivo.
 */
import { test } from '@playwright/test';

// Arquivo de seed para geração rápida de testes via Playwright codegen.
// Remova o .skip() e use "npx playwright codegen" para gravar os passos.
test.skip('seed', async ({ page: _page }) => {
  // Gere o código aqui usando: npx playwright codegen <url>
});
